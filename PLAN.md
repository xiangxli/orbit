# 历史星象功能：计划与现状

目标：输入任意时刻（公历/儒略历，支持公元前）与地点（经纬度），在现有 3D 场景中
显示该时该地的真实天空，星座连线用中国体系（三垣二十八宿），日月五星可查可看。

最后更新：2026-09-07（提交 ec6fd56）。

## 已定决策

| 项 | 决定 |
|---|---|
| 星座体系 | 只做中国体系（三垣二十八宿），数据来自 Stellarium `skycultures/chinese`，CC BY-SA，README 注明 |
| 历法输入 | 公历 + 儒略历（1582-10-15 前自动用儒略历），年份用天文纪年（0 = 公元前 1 年，-1 = 公元前 2 年）。不做农历/干支 |
| 时间范围 | 公元前 3000 到公元 3000。岁差用 Meeus 第 21 章（Lieske），此范围内角分级 |
| 行星 | 五大行星用 JPL 近似根数：1800–2050 用表 1，其余用表 2（木土加 M 的修正项）。天王海王不做 |
| 入口 | 页面打开直接进入今晚的真实星空（白天自动跳到 21:00），其上浮一张预填好的卡片（年代/地点，同 master 版式）：10 秒倒计时进度条走完自动关闭；卡片内任意交互即取消倒计时并隐藏进度条，标签页隐藏时暂停倒计时；年代预设（今晚 / 尧典 / 汉代 / 1054 天关客星）也收在面板"时间"区；不询问地点、不弹定位授权，默认西安、可在面板修改；演示模式保留，头部有切换 |
| 配色 | 冰蓝 `#8fc8ff` 强调色，深色玻璃面板，**不用黄色**（含天上的标签、选中圈） |
| 控件 | 不用浏览器原生外观：数字框去箭头、自绘复选框和滑杆、下拉换成自定义浮层菜单（原生 select 隐藏但保留值与 change 事件） |
| 方位 | 观测视角默认面南（方位 180°、仰角 40°、视场 70°），东在左、西在右，即"左青龙右白虎"。已用太阳实测：09:00 在屏幕左、15:00 在右 |
| 架构 | `astro.js` 纯函数做全部天文计算，输出 J2000 赤道系单位向量；`index.html` 只负责把向量映射进场景和 UI |
| 部署 | 仍支持直接打开 index.html。本地文件一律用经典 `<script src>`（file:// 下 Chrome 拦 ES module 和 fetch，不拦经典脚本）；银河贴图内联为 base64（file:// 下 `<img>` 会污染 WebGL 纹理）；脚本 `?v=` 防缓存，改了就升号 |
| 测试 | `node --test test/` 零依赖，25 项：Meeus 书中算例 + 真实天象（2024 日食、2020 木土大合、2022/2025 月掩火星、尧典中星） |

## 现状（按功能）

| 功能 | 状态 | 验证 |
|---|---|---|
| 天文核心 `astro.js` | ✅ 历法/JD、ΔT、恒星时、黄赤交角、岁差、自行、太阳（Meeus 25）、月亮（Meeus 47 全表）、五大行星（JPL）、月亮地心视差、地平坐标、月掩/凌/犯/合搜索 | `test/astro.test.js` 22 项，Meeus 例 7/12/13/21/22/25/33/47 到书中精度；2024-04-08 日食 0.35°；2020-12-21 木土 <0.25°；2022-12-08 伦敦、2025-01-14 纽约月掩火星命中并判为掩 |
| 数据 | ✅ `stars.js` HYG ≤6 等 + 连线用星共 5159 颗、2418 个中文星名；`constellations.js` 318 星官；`milkyway.js` NASA SVS 4851 银河（4k，sRGB，base64 1.6 MB）；`tools/build_data.py` 可重生成 | `test/sky.test.js`：数据一致性；2000 年极星北极星 0.74°、-2800 年右枢；《尧典》四仲中星 -2000 年西安昏时时角 5.7°/15.0°/9.6°/−12.2°（公元 0 年 12–38°） |
| 真实时钟接入场景 | ✅ JD 驱动轨道（真实日地距离）、pivot 姿态（当日天极/春分点）、自转（GMST）、月亮（真实方向与距离，相位来自光照）、行星；公转方向已改为逆时针 | 浏览器：2000-03-20 12:00 UT 日下点经度误差 1.85°（时差本身 1.9°）；地轴与当日天极偏差 0；北极星高度 = 纬度 |
| 观测视角 | ✅ 站在 (φ, λ)，地面圆盘遮挡 + 山脊剪影 + 地平辉光带（暮光朝太阳泛橙）+ 方位标；拖动环视、滚轮/捏合缩放、方向键、+/−；白天天空变蓝、星和标签淡出；银河在天文昏影以下才显示、放大时淡出 | 拖动 100 px 对应 15.8°（70° 视场）；太阳左东右西实测 |
| 星空渲染 | ✅ 星点按星等陡曲线 + B-V 颜色，≤2.5 等加光晕；放大时恒星最多放大 2 倍多、行星核心放大到 3 倍成小圆盘；月亮亮部提亮、暗部地照、广角有随月相变亮的光晕、"月"标签 | 截图：2026-08-15 22:00 西安朝南，箕宿/斗宿压在银心亮区，心宿/房宿在西侧 |
| 星官 | ✅ 连线；二十八宿名浅蓝常显，其余星官名视场 ≤45° 才显示 | |
| 点选 | ✅ 点星/行星弹出信息卡（中英名、星等、所属星官、高度方位、HIP、赤经赤纬；行星另有距离、距日、相角），天上有选中圈 | 真实点击命中大火 |
| 前往 | ✅ 太阳、月亮、天极、五星、二十八宿、北斗北极三垣、14 颗亮星，选中视线转过去 | |
| 月掩/凌/合 | ✅ 选行星按 ◀ ▶ 跳到当前地点上一次/下一次最近时刻（≤3°；掩 < 月面半径，凌 < 0.5°，犯 < 1°，其余为合）；可见判定为最近时刻前后 1.5 小时内月在地平线上且太阳低于 −3°；跳到最近时刻本身，若在地平线下信息卡标出，由用户自己拨时间；视场按角距自适应 | 2026-09-07 01:17 西安 月合火星 2.49° |
| 读数 | ✅ 当地恒星时、太阳高度方位、月相与月亮高度方位（含视差）、五星高度方位星等表、当代极星、视线方位/高度/视场 | |
| 时间控制 | ✅ 年月日时分、历法、当地平时/UTC、现在/±1h/±1d、年份拨杆与 ±1月/±1年/±100年（年步进按回归年，季节和时刻不变）、倍率滑杆、播放/暂停、带秒时钟；工具栏预设：实时 / 1 分钟每秒 / 斗转星移（20 分钟每秒）/ 岁差（每秒 100 年，按整回归年推进）/ 现在 / 今晚；白天进入自动跳 21:00 | 年份拨杆无昼夜闪烁 |
| 界面 | ✅ 欢迎卡片、玻璃面板呼吸微光、折叠分区（时间 / 地点 / 天象 / 显示 / 演示高级）、底部工具栏、自绘控件与下拉、手机底部抽屉（CSS 已写，真机未测） | localhost 预览确认 |
| 演示模式 | ✅ 保留原功能，收进高级区；真实模式下比例滑块置灰 | |

## 未做 / 已知粗糙处

1. ~~亮星常驻名字~~ ✅ 3 等以内有名字的星带标签，视场 >45° 只显示 1.5 等以内，20–45° 到 2.5 等，更近到 3 等。
2. ~~参考线~~ ✅ 子午线（地平系，随观测者）、当日天赤道（随岁差）、黄道（场景固定）三个开关，默认关。
3. ~~URL 分享~~ ✅ 真实模式下地址栏跟随 `?t=<JD>&lat&lon&az&alt&fov[&basis][&cal]`，工具栏"分享链接"复制；带参数打开直接进入且不跳"今晚"。注意 `applyUrlState()` 必须在模块末尾调用（它驱动场景，依赖后面声明的动画循环状态）。
4. 预设天象一键跳转（尧典四仲中星、1054 天关客星等）。
5. 阴历显示（只显示不输入）。
6. 月掩查找上限 30 年；远古年代 ΔT 本身有十几分钟不确定，月亮位置误差可达 0.2°，公元前的"掩/凌"判定不可靠。
7. 土星星等未算光环倾角（固定减 0.6 等）；行星未算光行时；不做章动、光行差、大气折射。
8. 手机布局只写了 CSS，没在真机看过。
9. 演示模式里的北斗、北极星仍是原来手摆的，与真实模式的星表是两套。
10. 银河贴图 4k，放大到 20° 以下视场会见到贴图颗粒，已用淡出规避。

## 文件

| 文件 | 作用 |
|---|---|
| `index.html` | 整个应用：three.js 场景、真实星空模式、面板与自绘控件 |
| `astro.js` | 天文计算，经典脚本 + CommonJS 导出 |
| `stars.js` / `constellations.js` / `milkyway.js` | 生成数据，`python3 tools/build_data.py /tmp/orbit-data` 重生成（源文件下载地址见脚本头部） |
| `test/astro.test.js` / `test/sky.test.js` | `node --test test/` |
| `.claude/launch.json` | 本地预览用（`python3 -m http.server 8791`），未入库 |

调试钩子：`window.__orbit`（real 状态、applyRealState、gotoTarget、pickStarAt、renderNow 等）。
注意：Claude 桌面面板里 file:// 打开的是静态快照，脚本不运行；浏览器面板隐藏时 Chrome 把 rAF 限到约 1 fps，自动化检查要用 `renderNow()` 强制出帧。

## 坐标约定（承重，改代码前先读）

### 场景坐标系

场景坐标系 = J2000 黄道坐标系的一个置换。设黄道系 (e_x, e_y, e_z)，e_x 指春分点，
e_z 指北黄极，e_y = 黄经 90°。则：

    scene.X = e_y    （黄经 90°，地球在 12 月冬至点的方向）
    scene.Y = e_z    （北黄极）
    scene.Z = e_x    （春分点）

右手系检验：X × Y = e_y × e_z = e_x = Z。`toScene(v)`：J2000 赤道向量先绕 x 转 −ε₀ 到黄道系，再置换 (y, z, x)。

推论：
- 太阳在原点。地球日心黄经 λ_E 处的位置 = R·(sin λ_E, 0, cos λ_E)，R 取真实日地距离。λ_E = λ☉ + 180°。
- 从 +Y 俯视，公转是逆时针：λ_E 增大时位置从 +X 转向 −Z。演示模式用 `(cos a, 0, −sin a)`，a = λ_E − 90°（原来的 `+sin a` 是顺时针，已改）。
- J2000 天极 = (sin ε₀, cos ε₀, 0)，即 `POLARIS_DIR`。

### 地球 pivot 局部系

真实模式下 `earthPivot` 的姿态由两个向量定义，用四元数设置：

    local.X = 当日春分点方向（当日赤经 0°）
    local.Y = 当日天极方向
    local.Z = X × Y

赤经 α 在局部系中为 (cos α, 0, −sin α)。`earth.rotation.y = GMST`（弧度）后，
格林尼治子午线（贴图 u = 0.5，即局部 +X）落在赤经 GMST 处。岁差由每帧重算 local.X / local.Y 自动得到。

演示模式仍用旧姿态（local.X 指向 J2000 赤经 90°，`rotation.z = −ε`），退出真实模式时恢复。

### 观测者与地平

地球网格上经度 λ（东正）、纬度 φ 的点，在 `earth` 局部系中：

    (cos φ cos λ, sin φ, −cos φ sin λ) · r

three.js SphereGeometry 的 u 增大方向对应贴图东向，对应局部 −Z（日下点实测核对）。

ENU：up = 观测者径向；north = 当日天极在地平面的投影；east = north × up。
视线 = cos(alt)·(cos(az)·north + sin(az)·east) + sin(alt)·up，az 从北顺时针。
观测视角下 `earthPivot` 隐藏，地面用一张贴在眼下 0.1 单位的不透明圆盘遮挡地平线以下的一切（月亮、太阳也被正确遮挡），山脊和辉光带在其上。

### 天球

星表、连线、标签、银河球、行星点都在 `skyGroup` 里，`skyGroup` 每帧平移到相机位置（skybox 手法），视差为零。半径 `R_SKY = 200000`，地平圆盘 180000，均在远平面 2e6 之内。
银河球：贴图为 NASA 赤道坐标 plate carrée，中心 0h，赤经向左增；three.js 从内看默认赤经向右，故 `repeat.x = −1`；网格局部系 X = 赤经 0、Y = 天极、Z = 赤经 −90°。
月亮不在天球上，而是在真实距离（60 地球半径）处的实体，观测者看到的方向自带视差。行星视差 <0.01°，放在天球上按地心方向画。

### 时间

- 唯一时间源：`real.jd`（UT 儒略日，浮点）。每帧 `jd += dt · rate / 86400`；"岁差"预设改为累积整回归年推进（`yearRate`）。
- 恒星时用 JD(UT)；太阳、月亮、行星、岁差、黄赤交角用 JD(TT) = JD(UT) + ΔT/86400。
- "当地时间"默认为当地平太阳时：UT = 当地时 − λ/15h。可切 UTC。
- 年步进按回归年 365.24219 天，季节与时刻不变；月步进按日历。

## 阶段与验收（历史记录）

### 阶段 0：天文核心 `astro.js` ✅

| 函数 | 算法 | 验收 |
|---|---|---|
| `calendarToJD`, `jdToCalendar` | Meeus 7.1 | 1957-10-04.81 → 2436116.31；333-01-27.5 儒略历 → 1842713.0；-4712-01-01.5 → 0 |
| `deltaT(year)` | Espenak-Meeus 多项式 | 2000 → 63.9 s；1000 → 1574 s；-1000 → 25400 s |
| `gmst(jd)` | Meeus 12.4 | 1987-04-10 0h UT → 197.693195°；19h21m UT → 128.737873° |
| `meanObliquity(jd)` | Meeus 22.3 Laskar | J2000 → 23.4392911°；1987-04-10 → 23°26′27.407″ |
| `precessionMatrix(jd)` | Meeus 21.2–21.4 | 例 21.b：θ Per 含自行后 2028-11-13.19 → α 2h46m11.331s δ +49°20′54.54″ |
| `sunPosition(jdTT)` | Meeus 25 低精度 | 例 25.a：1992-10-13 0h TD → ☉ 199.90988°，R 0.99766 |
| `moonPosition(jdTT)` | Meeus 47 全表 | 例 47.a：1992-04-12 0h TD → λ 133.162655° β −3.229126° Δ 368409.7 km |
| `planetVectorJ2000` | JPL 近似根数 | 例 33.a：金星 1992-12-20 0h TD 到 0.02°；2022-12-08 火星冲日与太阳黄经差 180°±0.1° |
| `moonTopocentricJ2000`, `findMoonPlanetApproach` | 地心视差 + 6 小时粗扫 + 黄金分割 | 2022-12-08 伦敦、2025-01-14 纽约月掩火星 |
| `eclToEq`, `eqToHorizontal` | 球面三角 | 例 13.b：Venus 1987-04-10 19:21 UT, Washington → A 68.0337° h 15.1249° |

不做：章动（9″）、光行差（20″）、大气折射。

### 阶段 1：真实时钟接入场景 ✅
验收：西安 2000-03-20 06:10 当地时太阳高度 ≈ 0°、方位 ≈ 90°（astro 测试）；北极星高度 = 纬度（浏览器）；日下点经度（浏览器）。

### 阶段 2：真实星空 ✅
验收：-1000 年帝星距天极 6.5°（4.5 等以内最近的是少尉 5.0°）；2000 年极星北极星 0.74°、-2800 年右枢 0.09°；《尧典》四仲中星 -2000 年西安昏时时角均 < 20°。

### 阶段 3：日月行星与读数 ✅（常驻星名标注未做）

### 阶段 4：打磨 🟡
已做：年份拨杆与步进、时钟与播放、前往、方向键、月掩查找、面板重做。未做：URL 分享、预设天象。
