# 历史星象功能实施计划

目标：输入任意时刻（公历/儒略历，支持公元前）与地点（经纬度），在现有 3D 场景中
显示该时该地的真实天空，星座连线用中国体系（三垣二十八宿）。

## 已定决策

| 项 | 决定 |
|---|---|
| 星座体系 | 只做中国体系（三垣二十八宿），数据来自 Stellarium `skycultures/chinese`，CC BY-SA 4.0，README 注明 |
| 历法输入 | 公历 + 儒略历（1582-10-15 前自动用儒略历），年份用天文纪年（0 = 公元前 1 年，-1 = 公元前 2 年）。不做农历/干支 |
| 时间范围 | 公元前 3000 到公元 3000。岁差用 Meeus 第 21 章（Lieske），此范围内角分级 |
| 行星 | 阶段 3，第一版不做 |
| 演示模式 | 保留。现有比例滑块可夸张的模式为"演示模式"；新功能为"真实模式"，进入后三个比例滑块锁 100，只留时间倍率 |
| 架构 | 路线 C：`astro.js` 纯函数做全部天文计算，输出 J2000 赤道系单位向量；`index.html` 只负责把向量映射进场景 |
| 部署 | 仍支持直接打开 index.html。本地文件一律用经典 `<script src>`（file:// 下 Chrome 拦 ES module 和 fetch，不拦经典脚本）。`astro.js` 带 CommonJS 导出守卫，同一份代码给浏览器和 node 测试用 |
| 测试 | `node --test test/` 零依赖。每个天文函数对照 Meeus《Astronomical Algorithms》书中算例 |

## 进度（2026-09-07）

| 阶段 | 状态 | 验证 |
|---|---|---|
| 0 天文核心 | 完成 `astro.js` | `test/astro.test.js` 18 项：Meeus 例 7/12/13/21/22/25/47 全部到书中精度；2024-04-08 日食日月地心角距 0.35°；右枢 −2800 年距极 0.09°，帝 −1100 年 6.5° |
| 1 真实时钟 | 完成 | 浏览器内：2000-03-20 12:00 UT 日下点经度误差 1.85°（时差本身 1.9°，证明贴图本初子午线 = 局部 +X、自转相位正确）；地轴与当日天极偏差 0.0000°；北极星高度 = 纬度（30.39° vs 30.27°） |
| 2 真实星空 | 完成 | `test/sky.test.js`：2000 年极星 = 北极星 0.74°，−2800 年 = 右枢；《尧典》四仲中星在 −2000 年西安昏时时角 5.7°/15.0°/9.6°/−12.2°，公元 0 年则 12–38°。浏览器截图见到星点、星官连线、星官名 |
| 3 日月行星与读数 | 部分：月亮真实位置、月相/恒星时/太阳高度/极星读数已做 | 行星、星名标注未做 |
| 4 打磨 | 未做 | |

未在浏览器里目视确认的（预览服务器中途被停）：观测视角下的地平线/地面遮挡/方位标、拖动环视、白天天空变蓝、点击地球选点。逻辑已写，需要人工打开页面过一遍。

## 坐标约定（承重，改代码前先读）

### 场景坐标系

场景坐标系 = J2000 黄道坐标系的一个置换。设黄道系 (e_x, e_y, e_z)，e_x 指春分点，
e_z 指北黄极，e_y = 黄经 90°。则：

    scene.X = e_y    （黄经 90°，地球在 12 月冬至点的方向）
    scene.Y = e_z    （北黄极）
    scene.Z = e_x    （春分点）

右手系检验：X × Y = e_y × e_z = e_x = Z。

推论：
- 太阳在原点。地球日心黄经 λ_E 处的位置 = R·(sin λ_E, 0, cos λ_E)。λ_E = λ☉ + 180°。
- 从 +Y 俯视，公转必须是逆时针：λ_E 增大时位置从 +X 转向 −Z。
  **现有代码 `(cos a, 0, sin a)` 是顺时针，接入真实时钟时改为 `(cos a, 0, −sin a)`，a = λ_E − 90°。**
- J2000 天极 = (sin ε₀, cos ε₀, 0)，与现有 `POLARIS_DIR` 一致。

### 地球 pivot 局部系

`earthPivot` 的姿态由两个向量定义，用四元数设置，不再用 `rotation.z = −ε`：

    local.X = 当日春分点方向（当日赤经 0°）
    local.Y = 当日天极方向
    local.Z = X × Y

赤经 α 在局部系中为 (cos α, 0, −sin α)。`earth.rotation.y = GMST`（弧度）后，
格林尼治子午线（贴图 u = 0.5，即局部 +X）落在赤经 GMST 处。
岁差由每帧重算 local.X / local.Y 自动得到。

演示模式仍用旧姿态（local.X 指向 J2000 赤经 90°，`rotation.z = −ε`），退出真实模式时恢复。
`goToToday` 在真实模式下改为把 `state.jd` 设为当前 UTC。

### 观测者

地球网格上经度 λ（东正）、纬度 φ 的点，在 `earth` 局部系中：

    (cos φ cos λ, sin φ, −cos φ sin λ) · r

three.js SphereGeometry 的 u 增大方向对应贴图东向，对应局部 −Z，已核对。
阶段 1 验收时用一个已知城市在地球贴图上的落点复核。

### 时间

- 唯一时间源：`state.jd`（UT 儒略日，浮点）。每帧 `jd += dt · rate / 86400`。
- 恒星时用 JD(UT)；太阳、月亮、岁差、黄赤交角用 JD(TT) = JD(UT) + ΔT/86400。
- 历史日期的"当地时间"默认为当地平太阳时：UT = 当地时 − λ/15h。可切 UTC。

### 天球

真实星表的 `Points` 放在一个每帧平移到相机位置的 Group 里（skybox 手法），
视差为零。现有 `SKY_RADIUS = 850000` 只有 36 AU，公转视差约 1.5°，不能复用。

## 阶段与验收

### 阶段 0：天文核心 `astro.js`

函数清单与验收算例（Meeus 章节 / 例号）：

| 函数 | 算法 | 验收 |
|---|---|---|
| `calendarToJD`, `jdToCalendar` | Meeus 7.1 | 1957-10-04.81 → 2436116.31；333-01-27.5 儒略历 → 1842713.0；-4712-01-01.5 → 0 |
| `deltaT(year)` | Espenak-Meeus 多项式 | 2000 → 63.9 s；1000 → 1574 s；-1000 → 25400 s（±1%） |
| `gmst(jd)` | Meeus 12.4 | 1987-04-10 0h UT → 197.693195°；19h21m UT → 128.737873° |
| `meanObliquity(jd)` | Meeus 22.2 Laskar | J2000 → 23.4392911°；1987-04-10 → 23°26′27.407″ |
| `precessionMatrix(jd)` | Meeus 21.2–21.4 | 例 21.b：θ Per 含自行后 2028-11-13.19 → α 2h46m11.331s δ +49°20′54.54″ |
| `applyProperMotion` | 线性 | 同上例中间值 α 2h44m12.975s δ 49°13′39.90″ |
| `sunPosition(jdTT)` | Meeus 25 低精度 | 例 25.a：1992-10-13 0h TD → ☉ 199.90988°，R 0.99766 |
| `moonPosition(jdTT)` | Meeus 47 全表 | 例 47.a：1992-04-12 0h TD → λ 133.162655° β −3.229126° Δ 368409.7 km |
| `eclToEq`, `eqToHorizontal` | 球面三角 | 例 13.b：Venus 1987-04-10 19:21 UT, Washington → A 68.0337° h 15.1249° |
| `sunVectorJ2000`, `moonVectorJ2000`, `poleOfDate`, `equinoxOfDate` | 组合 | 岁差：-1000 年天极离 β UMi（帝）< 8°；-2800 年离 α Dra（右枢）< 1° |

不做：章动（9″）、光行差（20″）、大气折射。

### 阶段 1：真实时钟接入场景

- 真实模式开关。开启后 `state.jd` 驱动轨道、自转、pivot 姿态；三个比例滑块锁 100。
- 输入：年/月/日/时/分 五个数字框 + 历法选择 + "现在"按钮；经度/纬度数字框 + 城市预设下拉。
- 观测者到 (φ, λ)，地平线圆 + 东南西北标记，拖动改方位/高度。
- 验收：
  1. 西安 2000-03-20 06:10 当地时，太阳高度 ≈ 0°，方位 ≈ 90°。
  2. 任意纬度北极星高度 = 纬度（当前年代）。
  3. 北京 2024-04-08 视角切到日月方向，日月角距 < 1°（当日日全食）。

### 阶段 2：真实星空

- 星表：HYG v3 裁到 6 等，字段 hip, ra, dec, mag, ci, pmra, pmdec，内联为 `stars.js`。
- 中国星名与连线：从 Stellarium chinese 提取，`constellations.js`。
- 星点用 `Points` + 按星等的大小/亮度 + B-V 颜色。
- 现有北斗、北极星、随机星点在真实模式下隐藏，由星表驱动。
- 验收：
  1. -1000 年帝星（北极二）距天极 < 8°（实测 6.5°；4.5 等以内离极最近的是少尉 5.0°）。
  2. 2000 年极星读数 = 北极星 0.74°；-2800 年 = 右枢 0.09°。
  3. 西安 -2000 春分日昏时子午线附近为星宿（《尧典》"日中星鸟"），四仲中星时角均 < 20°。

### 阶段 3：日月行星与读数

- 月亮按 `moonVectorJ2000` 摆到真实方向，距离按真实值缩放，相位由光照得出。
- 五大行星：JPL 近似轨道根数（3000 BC–3000 AD 版本）。
- 读数：当地恒星时、月相、当代极星名、子午线上的宿。
- 星名标注（中/英）。

### 阶段 4：打磨

- URL 状态 `?jd=…&lat=…&lon=…`。
- 预设事件：尧典四仲中星、1054 天关客星位置标记。
